/*import React from 'react'
import Person from './person';

 const Persons = (props) => {
    return(
        <div>
        {props.filterShow.map(person =>
          <Person key={person.name} person={person} number={person.number} handleRemove={props.removeName} />
          )}
      </div>
    )
}

export default Persons;
*/